# desktop

Windowsローカルで使うCodex入力補助を置く領域です。

現在の互換性維持のため、実際に利用されている `codex-cross-project-autohotkey-v2.ahk` と `install.ps1` はルート直下に残しています。このディレクトリは、将来AutoHotkey / PowerShell補助を整理するときの配置先です。

この領域はGitHub Actions自動レビュー・自動マージ本体とは分けて扱います。
