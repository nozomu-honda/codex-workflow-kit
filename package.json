{
  "name": "codex-workflow-kit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "test": "node --test",
    "lint": "node --check packages/chatgpt-automation-core/src/config/index.js && node --check packages/chatgpt-automation-core/test/config-validator.test.js && node --check scripts/validate-config.mjs",
    "validate:config": "node scripts/validate-config.mjs templates/chatgpt-automation.yml"
  },
  "dependencies": {
    "yaml": "^2.8.0"
  }
}
