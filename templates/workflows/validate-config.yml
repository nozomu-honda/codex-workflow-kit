name: Validate ChatGPT automation config

"on":
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate-config:
    name: Validate config
    permissions:
      contents: read
    uses: nozomu-honda/codex-workflow-kit/.github/workflows/validate-config.yml@REPLACE_WITH_TAG_OR_40_CHAR_COMMIT_SHA
    with:
      config-file: .github/chatgpt-automation.yml
      dry-run: true
