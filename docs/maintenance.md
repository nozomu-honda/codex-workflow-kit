# Maintenance

When changing prompts:

1. Keep hotstrings generic.
2. Do not add project-specific secrets or IDs.
3. Update README and docs if behavior changes.
4. Test `;ahtest` and `;cxgo` locally.
