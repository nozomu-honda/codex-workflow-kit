# Design

The kit is based on one rule:

Common workflow prompts live here.
Project-specific context lives in each project.

The AutoHotkey script therefore tells Codex to read project-local files such as `AGENTS.md` rather than embedding project details in the shared script.
